{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="section-{{ section.id }}-padding gradient color-{{ section.settings.section_color_scheme }} custom-section">
  <div class="page-width color-{{ section.settings.color_scheme }}">
  <div class="collection__product-wrapper">
    <div class="product-card__wrapper">
      <div class="product-card__img-wrapper">
        <img src="{{ section.settings.collection.featured_image |  img_url: 'medium' }}"  class="product-card__img">
      </div>
      <div class="product-card__info">
        <div class="product-card__rating">
          <div class="product-card__rating-star">
            {% render 'icon-star' %}
            {% render 'icon-star' %}
            {% render 'icon-star' %}
            {% render 'icon-star' %}
            {% render 'icon-star' %}
          </div>
          <span>{{ section.settings.collection.vendor }}</span>
        </div>
        <h6 class="product-card__title">{{ section.settings.collection.title }}</h6>
        <div class="product-card__info-footer">
          <span>{{ section.settings.collection.price |  money_with_currency }}</span>
          <button class="product-card__atc-btn"></button>
        </div>
      </div>
    </div>
  </div>

  </div>
</div>


{% schema %}
  {
    "name": "Feature Collection",
    "settings": [
      {
        "type": "inline_richtext",
        "id": "heading",
        "label": "Heading",
        "default": "Heading"
      },
      {
        "type": "color_scheme",
        "id": "section_color_scheme",
        "label": "t:sections.all.colors.label",
        "default": "scheme-1"
      },
      {
        "type": "product",
        "id": "collection",
        "label": "Collection"
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "t:sections.multirow.settings.container_color_scheme.label",
        "default": "scheme-1"
      },
      {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_top",
        "default": 36
      }, {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "t:sections.all.padding.padding_bottom",
        "default": 36
      }
    ]
  }
{% endschema %}